export let config = {
  nodes: {
    root: {
      root: `
        fixed top-0 w-screen items-center flex-wrap flex left-0 z-30
        p-0 h-16 dense:h-12 elevation-3 bg-$color-300 dark:bg-dark-600
      `
    }
  }
};

export default config;
