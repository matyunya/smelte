let config = {
  props: {
    color: "default",
    small: false,
    xs: false,
    reverse: false,
    tip: false
  },
  nodes: {
    root: {
      isMain: true,
      root: "material-icons icon text-xl transition",
      small: "text-small",
      xs: "text-xs",
      reverse: "reverse",
      tip: "tip"
    }
  }
};

export default config;
