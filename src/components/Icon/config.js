let config = {
  props: {
    color: "default",
    small: false,
    xs: false,
    reverse: false,
    tip: false
  },
  nodes: {
    root: {
      root: "material-icons icon text-xl duration-200 ease-in",
      small: "text-small",
      xs: "text-xs",
      reverse: "reverse",
      tip: "tip"
    }
  }
};

export default config;
